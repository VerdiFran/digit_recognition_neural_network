import {NeuralNetwork} from './neuralNetwork'
import {FileSource} from './fileSource'
import {MatrixBuilder} from './matrixBuilder'

export const calculate = (sourcePoints) => {
    let _network = new NeuralNetwork()


    //return _network.train(1, 1)
    //return _network.feedForward(_matrixBuilder.toUnitMatrix(sourcePoints))
}
